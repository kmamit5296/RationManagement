import styled from 'styled-components';
import { Container } from 'react-bootstrap';

export const HomePageStyle = styled(Container)`
  .table td {
    padding: 5px;
  }

  .edit-btn {
    padding: 0px 5px;
  }

  .first-col {
    width: 80px;
  }

  .table-head-wrapper {
    background-color: #cccccc;
  }
`;
